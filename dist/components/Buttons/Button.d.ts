import React from 'react';
import { ButtonProps } from './Button.types';
export default function Button({ children, onPress, onLongPress, palette, size, variant, rounded, active, disabled, style, }: ButtonProps): React.JSX.Element;
